sounddevice
google-genai
pillow
requests
beautifulsoup4
duckduckgo-search
playwright==1.62.0
pyautogui
pyperclip
pygetwindow; sys_platform == 'win32'
opencv-python
numpy
mss
Pillow
psutil
comtypes; sys_platform == 'win32'
pycaw; sys_platform == 'win32'
win10toast; sys_platform == 'win32'
send2trash
youtube-transcript-api
pywinauto; sys_platform == 'win32'
python-pptx
pyqt6
fastapi
uvicorn[standard]
qrcode[pil]
cryptography
pdfplumber
PyPDF2
python-docx
pandas
plyer
pydub
web3
pywin32; sys_platform == 'win32'
claude-agent-sdk==0.2.135  # GEMZ4US 2026-09-10: left unpinned, a delegated call's console
                           # window appeared on Sep 9 but not on two Sep 10 attempts — this
                           # SDK's own subprocess handling is the likely cause, and two builds
                           # made close together in time could easily have pulled different
                           # point releases with different internal behavior. Pinned to the
                           # version actually built and tested for v1.11.2 (see omni-os.spec's
                           # own APP_VERSION comment for why bundled versions matter here).
google-auth-oauthlib
google-api-python-client
msal
# Local, offline NSFW image classifier for actions/image_generator.py's
# post-generation safety gate — ships its own ONNX model (no download, no
# API key, no billing, no network call). Pulls in opencv-python-headless
# as a transitive dep; confirmed (2026-09-07) that installing it alongside
# the opencv-python already above does NOT conflict as long as pip resolves
# both to the same release version (it does, in one combined install) — if
# a future dependency bump ever skews them apart, that's the first thing to
# check if cv2 behaves oddly.
nudenet>=3.4.2
